<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.root.appbar">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".entrada"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Inicio"
            android:label="@string/inicio" />
        <activity
            android:name=".hoteles_mariscal"
            android:label="@string/hoteles" />
        <activity android:name=".ronda_ofertas"
            android:label="@string/ofertasR"/>
        <activity
            android:name=".MainActivity"
            android:label="@string/ofertas_mariscal" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAxs1YUcBrDeiSMgF4yx7unwGnX7I2yGBM" />

        <activity
            android:name=".llegar_mariscal"
            android:label="@string/title_activity_llegar_mariscal" />
        <activity android:name=".restaurante_mariscal"
            android:label="@string/restaurante_mariscal"/>


        <activity android:name=".precios_list" />
        <activity
            android:name=".fotos_locales"
            android:label="@string/discovery" />

        <activity android:name=".llegar_discovery" />

        <activity android:name=".hotel_plaza"
            android:label="@string/hotel_plaza"/>
        <activity android:name=".llegar_plaza" />

        <activity android:name=".siete_chiles_res"
            android:label="@string/siete_chiles_res"/>
        <activity android:name=".llegar_siete" />

        <activity android:name=".res_la_pepa"
            android:label="@string/res_la_pepa"/>
        <activity android:name=".llegar_pepa" />

        <activity android:name=".rock_your_dogs"
            android:label="@string/rock_your_dogs"/>
        <activity android:name=".llegar_dogs" />

        <activity android:name=".res_burguer_clan"
            android:label="@string/res_burguer_clan"/>
        <activity android:name=".llegar_clan" />

        <activity android:name=".res_flora"
            android:label="@string/res_flora"/>
        <activity android:name=".llegar_flora" />

        <activity android:name=".bares_mariscal"
            android:label="@string/bares_mariscal"/>

        <activity android:name=".bar_space_entertaiment"
            android:label="@string/bar_space_entertaiment"/>
        <activity android:name=".llegar_space" />

        <activity android:name=".bar_absin_the"
            android:label="@string/bar_absin_the"/>
        <activity android:name=".llegar_absin" />

        <activity android:name=".bar_casa_eva"
            android:label="@string/bar_casa_eva"/>
        <activity android:name=".llegar_eva" />

        <activity android:name=".llegar_ronda" />
        <activity android:name=".res_ronda"
            android:label="@string/res_ronda"/>

        <activity android:name=".res_geranios"
            android:label="@string/res_geranios"/>
        <activity android:name=".llegar_geranios" />

        <activity android:name=".res_fogones"
            android:label="@string/res_fogones"/>
        <activity android:name=".llegar_fogones" />

        <activity android:name=".res_pozo"
            android:label="@string/res_pozo"/>
        <activity android:name=".llegar_pozo" />

        <activity android:name=".bar_anoranza"
            android:label="@string/bar_anoranza"/>
        <activity android:name=".llegar_anoranza" />

        <activity android:name=".bares_ronda"
            android:label="@string/bares_ronda"/>

        <activity android:name=".bar_el_javi"
            android:label="@string/bar_el_javi"/>
        <activity android:name=".llegar_el_javi" />

        <activity android:name=".bar_expreso_ronda"
            android:label="@string/bar_expreso_ronda"/>
        <activity android:name=".llegar_expreso" />

        <activity android:name=".bar_murcielago"
            android:label="@string/bar_murcielago"/>
        <activity android:name=".llegar_murcielago"/>

    </application>

</manifest>